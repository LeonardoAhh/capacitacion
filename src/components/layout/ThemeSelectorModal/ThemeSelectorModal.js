// ThemeSelectorModal — Temporalmente desactivado
// El selector de tema está bloqueado. Para reactivarlo, restaurar el componente original.

export default function ThemeSelectorModal({ isOpen, onClose, currentTheme, onSelectTheme }) {
    return null;
}